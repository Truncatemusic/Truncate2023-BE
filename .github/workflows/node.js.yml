# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI BE

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Transfer Secret to Server
      run: |
        echo "${{ secrets.SERVER_SSH_KEY }}" > ./private_key
        chmod 600 ./private_key
        echo "${{ secrets.SETUP_ENV }}" > ./.env
        scp -o StrictHostKeyChecking=no -i ./private_key ./.env ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_HOST }}:/home/Truncate/actions-runner/Truncate2023-Be/Truncate2023-BE/Truncate2023-BE/Truncate2023-BE/.env

    - name: Install Dependencies
      run: npm i

    - name: Migrate Database
      run: npm run migrate

    - name: Pull DB
      run: npx prisma db pull

    - name: Generate Prisma Client
      run: npx prisma generate

    - name: Build Nest.js Application
      run: npm run build

    - name: Restart Backend Api
      run: pm2 restart Truncate-Backend

