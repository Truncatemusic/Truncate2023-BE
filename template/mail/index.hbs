<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <style>{{{ css }}}</style>
        <title>{{ subject }}</title>
    </head>
    <body>
        {{{ header }}}
        <main>
            <div class="box template-{{ template }}">{{{ body }}}</div>
        </main>
        {{{ footer }}}
    </body>
</html>